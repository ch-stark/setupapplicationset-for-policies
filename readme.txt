* clone this repository
* assuming you have a RHACM 2.4.1 HubCluster
* label the cluster with environment=dev
* execute the policies to setup all resources
* once deployed you have an ApplicationSet deployed on local-cluster
* you can login to ArgoCD using OpenShift Credentials

* it will deploy automatically a policy
